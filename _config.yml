# Jekyll config for GitHub Pages.
# The site is a static SPA; we only need Jekyll for the `exclude:` list so
# certain files stay in git but aren't copied into the deployed site.

exclude:
  - og-preview.png
  - LICENSE
  - README.md
  - TODO.md
